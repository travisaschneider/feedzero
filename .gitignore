node_modules/
dist/
extension/dist/
coverage/
.vitest-reports/
.env
.env.*
!.env.example
test-results/
playwright-report/
# Personal Claude Code config + permissions, not for the public repo.
# Skills, hooks, and commands ARE tracked because they describe shared
# workflows (e.g. /new-release, /research-competitors) and contributor
# automation (RGR pre-edit hook).
.claude/*
!.claude/skills/
!.claude/hooks/
!.claude/commands/

.vercel
.env*.local

# Private operational/strategy docs — not for the public repo
docs/internal/

# Claude Code skill manifest — local state, not for the public repo
skills-lock.json

# Output of scripts/take-screenshot.js — regenerated on demand
feedzero-screenshot.png
